// import html2pdf from 'html-pdf';
// import Promise from 'bluebird';
// import path from 'path';
// import archive from './config';
//
//
// export default (html, filePath, config = {}) => new Promise((resolve, reject) => {
//     html2pdf.create(html, config).toFile(path.join(archive.archive, filePath), (err, response) => {
//         if (err) {
//             return reject(err);
//         }
//         return resolve(response);
//     });
// });
